<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- For the list selector to work properly with ActionBarSherlock pre ICS, the selector must be a drawable, not a color -->
    <item android:drawable="@drawable/black_twenty" android:state_pressed="true"/>
    <item android:drawable="@android:color/transparent"/>

</selector>
